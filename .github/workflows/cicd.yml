name: Build and Push to ECR

on:
  push:
    branches:
      - main

jobs:
  build-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        run: |
          aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/w4l9y5r3

      - name: Build, tag, and push image to Amazon ECR
        run: |
          docker build -t docker-ecr:v2 .
          docker tag docker-ecr:v2 public.ecr.aws/w4l9y5r3/docker-ecr:v2
          docker push public.ecr.aws/w4l9y5r3/docker-ecr:v2

  deploy:
    runs-on: ubuntu-latest
    needs: build-push
    steps: 
      - name: check my ssh key
        run: |
          echo "${{ secrets.SSH_KEY }}" > mykey.pem
          chmod 600 mykey.pem
          cat mykey.pem
        

          ssh -o StrictHostKeyChecking=no -i mykey.pem ubuntu@204.236.208.78 "docker pull public.ecr.aws/w4l9y5r3/docker-ecr:v2"

          ssh -o StrictHostKeyChecking=no -i mykey.pem ubuntu@204.236.208.78 "docker run -d -p 80:80 public.ecr.aws/w4l9y5r3/docker-ecr:v2"

          